// pages/settings.js
import { Container, Typography } from '@mui/material';

const SettingsPage = () => {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>
        Settings
      </Typography>
      <Typography variant="body1">
        Here you can adjust your account settings and preferences.
      </Typography>
    </Container>
  );
};

export default SettingsPage;
